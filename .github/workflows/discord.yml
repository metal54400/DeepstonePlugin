name: Annonce update plugin

on:
  release:
    types: [published]

jobs:
  announce:
    runs-on: ubuntu-latest
    steps:
      - name: Envoyer l'annonce Discord
        run: |
          VERSION="${{ github.event.release.tag_name }}"
          URL="${{ github.event.release.html_url }}"
          CHANGELOG="${{ github.event.release.body }}"

          curl -H "Content-Type: application/json" \
               -d "$(jq -n --arg content "📢 **Nouvelle mise à jour du plugin DeepstonePlugin**\n\n🧩 Version : $VERSION\n📝 Changelog :\n$CHANGELOG\n\n🔗 $URL" '{content: $content}')" \
               "https://discord.com/api/webhooks/1470481645449510946/46pIUNdqayUcEzrANvUyFhyuWXuJaetAfywveyr1mTScLOF762-oWoe3Ye-tk40GCihO"
